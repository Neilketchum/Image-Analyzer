<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../styes.css"> <!-- Link to external CSS -->

    <title>Image Analysis Result</title>
    <style>
        body {
            display: flex;
            justify-content: space-around;
            align-items: center;
        }
        img {
            max-width: 300px; /* Set a max width for the image */
            max-height: 300px; /* Set a max height for the image */
        }
        .results {
            margin-left: 20px;
        }
    </style>
</head>
<body>
    <h1>Image Analysis Result</h1>


    <div class="results">
        <h2>Detected Labels:</h2>
        <ul>
            <% labels.forEach(label => { %>
                <li><%= label.description %> (Confidence: <%= (label.score * 100).toFixed(2) %>%)</li>
            <% }) %>
        </ul>
    </div>

    <button onclick="window.history.back();">Back</button>
</body>
</html>
